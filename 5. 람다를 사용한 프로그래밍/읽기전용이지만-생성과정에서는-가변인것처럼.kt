val fibonacci = buildList {
    addAll(listof (1, 1, 2))
    add (3)
    add index = 0, element = 3)
    }

val shouldAdd = true

val fruits = buildSet {
    add ("Apple")
    if (shouldAdd) {
        addA11(listof ("Apple", "Banana", "Cherry"))
    }
}
val medals = buildMap<String, Int> {
    put ("Gold", 1)
    putAll(listof("Silver" to 2, "Bronze" to 3))
}